# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: bill.spec.js >> Block 5 — Navigation >> TC-BILL-15 | Bill pay page accessible via left nav link
- Location: tests/bill.spec.js:128:3

# Error details

```
TimeoutError: locator.waitFor: Timeout 25000ms exceeded.
Call log:
  - waiting for locator('a[href*="billpay"]') to be visible

```

# Page snapshot

```yaml
- generic [ref=e3]:
  - banner [ref=e4]:
    - heading "Error 1015" [level=1] [ref=e5]
    - generic [ref=e6]: "Ray ID: a02ef7f82c98eb31 •"
    - generic [ref=e7]: 2026-05-28 17:27:11 UTC
    - heading "You are being rate limited" [level=2] [ref=e8]
  - generic [ref=e10]:
    - heading "What happened?" [level=2] [ref=e11]
    - paragraph [ref=e12]: The owner of this website (parabank.parasoft.com) has banned you temporarily from accessing this website.
    - paragraph [ref=e13]:
      - text: Please see
      - link "https://developers.cloudflare.com/support/troubleshooting/http-status-codes/cloudflare-1xxx-errors/error-1015/" [ref=e14] [cursor=pointer]:
        - /url: https://developers.cloudflare.com/support/troubleshooting/http-status-codes/cloudflare-1xxx-errors/error-1015/
      - text: for more details.
  - generic [ref=e16]:
    - text: Was this page helpful?
    - button "Yes" [ref=e17] [cursor=pointer]
    - button "No" [ref=e18] [cursor=pointer]
  - paragraph [ref=e20]:
    - generic [ref=e21]:
      - text: "Cloudflare Ray ID:"
      - strong [ref=e22]: a02ef7f82c98eb31
    - text: •
    - generic [ref=e23]:
      - text: "Your IP:"
      - button "Click to reveal" [ref=e24] [cursor=pointer]
      - text: •
    - generic [ref=e25]:
      - text: Performance & security by
      - link "Cloudflare" [ref=e26] [cursor=pointer]:
        - /url: https://www.cloudflare.com/5xx-error-landing
```

# Test source

```ts
  33  |     const bill = await loginAndGoto(page);
  34  |     await expect(bill.rightPanel).toBeVisible();
  35  |   });
  36  |   test('TC-BILL-03 | Bill pay page title contains relevant text', async ({ page }) => {
  37  |     const bill = await loginAndGoto(page);
  38  |     await expect(async () => {
  39  |       const text = await bill.getRightPanelText();
  40  |       expect(text.toLowerCase()).toMatch(/bill|payment|payee/);
  41  |     }).toPass({ timeout: 15000 });
  42  |   });
  43  | });
  44  | 
  45  | test.describe('Block 2 — Form Fields', () => {
  46  |   test('TC-BILL-04 | Payee name input is visible', async ({ page }) => {
  47  |     const bill = await loginAndGoto(page);
  48  |     await expect(bill.payeeNameInput).toBeVisible();
  49  |   });
  50  |   test('TC-BILL-05 | Address input is visible', async ({ page }) => {
  51  |     const bill = await loginAndGoto(page);
  52  |     await bill.addressInput.waitFor({ state: 'visible', timeout: 15000 });
  53  |     await expect(bill.addressInput).toBeVisible();
  54  |   });
  55  |   test('TC-BILL-06 | Amount input is visible', async ({ page }) => {
  56  |     const bill = await loginAndGoto(page);
  57  |     await bill.amountInput.waitFor({ state: 'visible', timeout: 15000 });
  58  |     await expect(bill.amountInput).toBeVisible();
  59  |   });
  60  |   test('TC-BILL-07 | Account number input is visible', async ({ page }) => {
  61  |     const bill = await loginAndGoto(page);
  62  |     await bill.accountInput.waitFor({ state: 'visible', timeout: 15000 });
  63  |     await expect(bill.accountInput).toBeVisible();
  64  |   });
  65  |   test('TC-BILL-08 | Send Payment button is visible', async ({ page }) => {
  66  |     const bill = await loginAndGoto(page);
  67  |     await bill.sendButton.waitFor({ state: 'visible', timeout: 15000 });
  68  |     await expect(bill.sendButton).toBeVisible();
  69  |   });
  70  | });
  71  | 
  72  | test.describe('Block 3 — From Account', () => {
  73  |   test('TC-BILL-09 | From account dropdown is visible', async ({ page }) => {
  74  |     const bill = await loginAndGoto(page);
  75  |     await bill.fromAccountSelect.waitFor({ state: 'visible', timeout: 20000 });
  76  |     await expect(bill.fromAccountSelect).toBeVisible();
  77  |   });
  78  |   test('TC-BILL-10 | From account dropdown loads accounts', async ({ page }) => {
  79  |     const bill = await loginAndGoto(page);
  80  |     await waitForFromAccount(bill);
  81  |     const count = await bill.getFromAccountCount();
  82  |     expect(count).toBeGreaterThanOrEqual(1);
  83  |   });
  84  | });
  85  | 
  86  | test.describe('Block 4 — Payment Submit', () => {
  87  |   test('TC-BILL-11 | Submitting valid payment shows response', async ({ page }) => {
  88  |     const bill = await loginAndGoto(page);
  89  |     await waitForFromAccount(bill);
  90  |     await bill.fillPayeeDetails(TEST_DATA.payeeData);
  91  |     await bill.sendButton.click();
  92  |     await page.waitForLoadState('domcontentloaded');
  93  |     await expect(async () => {
  94  |       const content = await bill.getRightPanelText();
  95  |       expect(content.length).toBeGreaterThan(0);
  96  |     }).toPass({ timeout: 20000 });
  97  |   });
  98  |   test('TC-BILL-12 | Payment response contains payment related text', async ({ page }) => {
  99  |     const bill = await loginAndGoto(page);
  100 |     await waitForFromAccount(bill);
  101 |     await bill.fillPayeeDetails(TEST_DATA.payeeData);
  102 |     await bill.sendButton.click();
  103 |     await expect(async () => {
  104 |       const content = await bill.getRightPanelText();
  105 |       expect(content.toLowerCase()).toMatch(/payment|bill|complete|error/);
  106 |     }).toPass({ timeout: 20000 });
  107 |   });
  108 |   test('TC-BILL-13 | Submitting empty form shows page response', async ({ page }) => {
  109 |     const bill = await loginAndGoto(page);
  110 |     await bill.sendButton.click();
  111 |     await page.waitForLoadState('domcontentloaded');
  112 |     await expect(async () => {
  113 |       const content = await bill.getRightPanelText();
  114 |       expect(content.length).toBeGreaterThan(0);
  115 |     }).toPass({ timeout: 20000 });
  116 |   });
  117 | });
  118 | 
  119 | test.describe('Block 5 — Navigation', () => {
  120 |   test('TC-BILL-14 | Bill pay link is visible in left nav after login', async ({ page }) => {
  121 |     const auth = new AuthPage(page);
  122 |     await auth.login(TEST_DATA.validUser.username, TEST_DATA.validUser.password);
  123 |     await page.waitForLoadState('domcontentloaded');
  124 |     const bill = new BillPage(page);
  125 |     await bill.billPayLink.waitFor({ state: 'visible', timeout: 25000 });
  126 |     await expect(bill.billPayLink).toBeVisible();
  127 |   });
  128 |   test('TC-BILL-15 | Bill pay page accessible via left nav link', async ({ page }) => {
  129 |     const auth = new AuthPage(page);
  130 |     await auth.login(TEST_DATA.validUser.username, TEST_DATA.validUser.password);
  131 |     await page.waitForLoadState('domcontentloaded');
  132 |     const bill = new BillPage(page);
> 133 |     await bill.billPayLink.waitFor({ state: 'visible', timeout: 25000 });
      |                            ^ TimeoutError: locator.waitFor: Timeout 25000ms exceeded.
  134 |     await bill.billPayLink.click();
  135 |     await page.waitForLoadState('domcontentloaded');
  136 |     await expect(page).toHaveURL(/billpay/);
  137 |   });
  138 | });
```